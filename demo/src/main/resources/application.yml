spring:
  thymeleaf:
    prefix: classpath:templates/
    check-template-location: true
    mode: HTML5
    suffix: .html
    cache: false

  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/admin?serverTimezone=UTC&charaterEncoding=UTF-8&useSSL=false
    username: root 
    password: rharha0208 
  jpa:
    show-sql: true
    database: mysql
